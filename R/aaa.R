the <- new_environment()
the$server_processes <- list()
the$http_allowed_origins <- character()
the$http_trusted_hosts <- character()
the$http_shared_secret <- NULL
the$socket_secret <- NULL
the$server_tool_output_schemas <- list()
